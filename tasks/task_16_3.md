# Задача 16.3: UI: full-text turns, expand-on-tap logs, short model tags

## Связь с юзер-кейсами
- UC-02: Observe a live public session
- UC-07: Reconnect and replay
- UC-12: Mobile-native UI

## Цель задачи
Сделать отображение реплик полностью читабельным (без обрезки), добавить разворачиваемые логи и краткие теги моделей для Researcher/Subject.

## Описание изменений

### Изменения в существующих файлах

#### Файл: `apps/web/src/App.tsx`
- Добавить раскрытие/сворачивание карточек лога по тапу.
- Убедиться, что длинные ответы полностью отображаются в Live/Logs (без ellipsis).
- Отображать короткие model tags рядом с лейблами ролей (использовать поле из StreamEvent).

#### Файл: `apps/web/src/hooks/useStream.ts`
- Расширить тип StreamEvent (например, `model_tag`) при наличии данных в стриме.

#### Файл: `apps/web/src/index.css`
- Добавить стили для expanded-состояния логов и мягкого градиента для свернутого вида.

#### Файл: `apps/web/src/i18n.ts`
- Добавить/обновить подписи для модели и состояния раскрытия.

## Тест-кейсы

### E2E тесты
1. **TC-E2E-16.3-01:** Логи разворачиваются по тапу и показывают полный текст.
2. **TC-E2E-16.3-02:** В карточках отображаются короткие теги моделей.

### Регрессионные тесты
- Playwright E2E (см. задачу 16.4).

## Критерии приёмки
- [ ] Длинные ответы полностью видимы в Live/Logs без обрезки.
- [ ] Логи разворачиваются по тапу и сохраняют состояние при навигации.
- [ ] Короткие model tags отображаются рядом с лейблами ролей.
