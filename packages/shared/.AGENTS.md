# Directory: packages/shared

## Purpose
Shared package consumed by the web, server, and worker apps.

## Files
- package.json — package metadata and build script.
- tsconfig.json — TypeScript config for shared build.
- dist/ — compiled outputs (generated).
- src/ — shared source types and helpers.
