# Directory: apps/worker

## Purpose
Background worker for running Researcher/Subject turns and publishing stream events.

## Files
- package.json — workspace scripts for dev/build/test.
- tsconfig.json — TypeScript build configuration.
- src/ — worker runtime modules (runner, telemetry, streaming, safety, model clients).
