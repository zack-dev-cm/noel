# Directory: apps/worker/src

## Purpose
Worker runtime entry points and pipeline modules.

## Files
- index.ts — service bootstrap.
- runner.ts — session loop, telemetry, and stream emission.
- telemetry/ — telemetry derivation helpers.
- stream/ — stream publishing abstraction.
- openai/ — OpenAI client and usage tracking.
- gemini/ — Gemini client and context formatting.
- safety/ — moderation checks and thresholds.
- admin/ — admin settings fetch.
- interventions/ — intervention queue handling.
- observability/ — structured logging and traces.
- utils/ — retry and text helpers.
